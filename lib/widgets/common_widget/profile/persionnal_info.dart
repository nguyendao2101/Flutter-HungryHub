import 'package:flutter/material.dart';

class PersionnalInfo extends StatelessWidget {
  const PersionnalInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Persional Info'),
      ),
    );
  }
}
